---

---

<header>
  <div class="hamburger_menu">
    <input id="menu_toggle" type="checkbox" />
    <label class="menu_knap" for="menu_toggle">
      <span></span>
    </label>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Biryani:wght@200;300;400;600;700;800;900&family=Josefin+Sans:ital,wght@0,100..700;1,100..700&family=Prata&display=swap"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0"
    />
    <ul>
      <li id="logo" class="logo">
        <a href="/"><div class="logo_img"></div></a>
      </li>
      <li><a href="/listeveiw">Sko</a></li>
      <li><a href="/sissel_edelbo">Sissel Edelbo</a></li>
      <li><a href="/omk2">Om k2</a></li>
      <li><a href="/kontakt">Kontakt</a></li>
      <li>
        <form class="right_search">
          <div class="search">
            <span class="material-symbols-outlined"> search </span>
            <input class="search-input" type="search" placeholder="Søg" />
          </div>
        </form>
      </li>
      <li class="right kurv">
        <a href="/kurv"><img src="/img/kurv.svg" alt="billede af en kurv" /></a>
      </li>
    </ul>
  </div>
</header>

<style>
  /******** Brugermenu ************/

  @media (max-width: 800px) {
    .hamburger_menu ul {
      display: flex;
      flex-direction: column;
      align-items: flex-start;
      justify-content: flex-start;
      position: fixed;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      margin: 0;
      padding: 0 40px;
      background-color: var(--offWhite);
      list-style-type: none;
    }

    li {
      padding: 10px 0;
    }
    #menu_toggle:checked ~ ul {
      left: 0;
    }

    .menu_knap span {
      position: fixed;
      top: 5px;
      left: 5px;
      width: 50px;
      height: 50px;
      cursor: pointer;
      z-index: 20;
      background-size: cover;
      background-image: url(/img/logo.svg);
    }

    /**** Skjuler checkbox ****/
    #menu_toggle {
      opacity: 0;
    }

    /**** Når man klikker på burgermenu laver den kryds ****/
    #menu_toggle:checked + .menu_knap > span {
      transform: rotate(45deg);
    }
    /*Samlet set betyder > span i dette kontekst, at CSS-reglen vil blive anvendt på <span>-elementet, der er et direkte barn af .menu_knap (label-elementet), når checkboxen med id "menu_toggle" er afkrydset (checked).*/

    #menu_toggle:checked + .menu_knap > span::before {
      top: 0;
      transform: rotate(0deg);
    }
    /*Samlet set betyder #menu_toggle:checked + .menu_knap > span::before, at CSS-reglen vil blive anvendt på pseudo-elementet ::before på <span>-elementet, som er et direkte barn af .menu_knap (label-elementet), når checkboxen med id "menu_toggle" er afkrydset (checked). Dette er ofte brugt til at skabe ekstra dekorative elementer, såsom linjer eller symboler, før indholdet af det valgte element.*/

    #menu_toggle:checked + .menu_knap > span::after {
      top: 0;
      transform: rotate(90deg);
    }
    /*Samlet set betyder #menu_toggle:checked + .menu_knap > span::after, at CSS-reglen vil blive anvendt på pseudo-elementet ::after på <span>-elementet, som er et direkte barn af .menu_knap (label-elementet), når checkboxen med id "menu_toggle" er afkrydset (checked). Dette kan bruges til at tilføje yderligere dekoration eller stil til "hamburger"-ikonet efter det faktiske indhold af <span>.*/

    /*Viser selve menuen ****/
    #menu_toggle:checked ~ ul {
      left: 0 !important;
    }
    /*Dette er "general sibling combinator" og vælger alle efterfølgende søskende elementer af det tidligere valgte element, der deler samme forælder.*/

    /*Udsseende af burgermenu knappen ****/
    .menu_knap {
      position: fixed;
      top: 20px;
      left: 20px;
      width: 26px;
      height: 26px;
      cursor: pointer;
      z-index: 20;
    }

    /**** Viser burgermenu med de 3 streger */
    .menu_knap > span,
    .menu_knap > span::before,
    .menu_knap > span::after {
      display: block;
      position: absolute;
      width: 100%;
      height: 2px;
      background-color: var(--offBlack);
    }

    .menu_knap > span::before {
      content: "";
      top: -8px;
    }
    .menu_knap > span::after {
      content: "";
      top: 8px;
    }
  }
  .kurv {
    width: 30px;
    height: 25px;
  }

  .kurv img:hover {
    border: none;
  }

  #menu_toggle {
    display: none;
  }

  .hamburger-menu label,
  .hamburger-menu input {
    position: absolute;
    display: none;
    z-index: 20;
  }

  .menu_knap span {
    background-image: url(/img/logo.svg);
  }

  header {
    position: sticky;
    top: 0;
    z-index: 20;
  }

  header a {
    text-decoration: none;
  }

  .logo {
    padding-left: 15px;
    align-self: center;
  }
  .logo li {
    font-size: 0;
  }

  .logo_img {
    display: block;
    background-image: url(/img/logo.svg);
    width: 100px;
    height: 100px;
    background-repeat: no-repeat;
    background-size: contain;
  }

  .aktiv {
    border-bottom: solid 2px var(--lyserRoed600);
  }
  .right {
    position: absolute;
    right: 2%;
  }

  /****************************** Søge funktion ******************************/
  .search {
    width: max-content;
    display: flex;
    align-items: center;
    padding: 5px;
    border-radius: 3px;
    background: var(--offWhite);
    border: 2px solid var(--offBlack);
  }

  .search-input {
    color: var(--offBlack);
    margin-left: 5px;
    outline: none;
    border: none;
    background: transparent;
    width: 200px;
  }

  /******* Desktop **********/
  @media (min-width: 800px) {
    ul {
      display: grid;
      grid-auto-flow: column;
      justify-content: start;
      align-items: center;
      list-style-type: none;
      gap: 35px;

      position: relative;
    }

    #logo :hover {
      border: none;
    }
    li :hover {
      border-bottom: solid 2px var(--lyserRoed600);
    }

    header {
      background-color: var(--offWhite);
    }

    .right {
      position: absolute;
      right: 2%;
    }

    .right_search {
      position: absolute;
      right: 6%;
    }
  }
</style>
